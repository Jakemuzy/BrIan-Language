fn int func(int a, int b) {
/*

    (a + b)++;
    a += 2; 
*/


//let int a[2][3] = {{1, 2, 3}, {3, 2, 1}};
//let int result = a[1][2];

//let int size = sizeof(int);
//let int size2 = sizeof(WHAT);

/* 
type t = int;
type t = float; // Err
let t tdtest = 4;
*/

/*
let int RED = true;
enum COLOR {
    RED, GREEN, BLUE    // Err red defined
};
let int BLUE = true;    // Err blue defined
*/

/*
enum DUPE {
    JUAN, Two, JUAN
};
*/

/* 
interface Printable {
    let int print;
    fn void print(string msg);  // Already defined
}
*/

/*
let int a;
interface Printable {
    let int a;
    fn void print(string msg);  
}
*/

/*
interface Printable {
    fn void print(string msg);  
}
*/

/*
enum BUDDY {
    John, Harry, Calvin
};

let int b = 3;
struct Group : Printable {
    let BUDDY one, two, three = Calvin;

    fn void print(string msg) {
        // ...
        three = two;
    }

    operator +(Group a, Group b) {
        return b;
    }
}
*/

/*
struct GenGroup <T, B> {
    let T one;
    let B two;

    fn void ADD(T one, B two);
}
fn void ADD(int a, int b);
*/

/*
struct GenGroup2 <T, B> {
    fn void ADD(T one, B two) // Err, decl only
    {

    }
}
*/

/*
struct Nest {
    struct two {    // Err

    }
}
*/

/*
struct EnumNest {
    enum NAMES {
        JOE, BIDEN, KAMALA
    };

    let int a = JOE;
}
*/


/*
fn void CriticalTest() {
    let int i = 0;
    
    lock ( i = i++ ) {

    }

    lock () {   // Err
        
    }

    critical {

        break;
        continue;
    }
}
*/

/*
type ID = int;
let int testerX = 1;
let chan<int> intChan = <-(testerX + 1);
let chan<ID> idChan;
*/

let int x = 3;
let int y = 3;
let mat<x,y> mat1; 
let mat<1,y> mat2; 
let mat<x,1> mat3; 

/*
let int size = 3;
let vec<1> vec1;
let vec<size> vec2;
*/


//let bool isTrue = 1 ? true : false;